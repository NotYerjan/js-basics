function canPlay() {
  let sport = " Football";

  if (true) {
    let personName = "Cosimo";
    console.log(personName + sport);
  }
}

// personName can be called only in the if satetement

canPlay();
